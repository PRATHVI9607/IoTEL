[project]
name = "iotel-attacker"
version = "0.1.0"
description = "IoTEL Attacker scripts for IDP security research"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.28",
    "scapy>=2.5",
    "colorama>=0.4.6",
]

[tool.uv]
package = false
