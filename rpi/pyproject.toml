[project]
name = "iotel-rpi-bridge"
version = "0.1.0"
description = "IoTEL RPI Drone Bridge"
requires-python = ">=3.11"
dependencies = [
    "pymavlink>=2.4.40",
    "pyserial>=3.5",
    "requests>=2.28",
]

[tool.uv]
package = false
